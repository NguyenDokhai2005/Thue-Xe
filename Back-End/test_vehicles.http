### 0) Tạo admin mặc định (nếu chưa có)
POST http://localhost:8080/api/admin-setup/create-default-admin

### 1) Đăng nhập admin để lấy token
POST http://localhost:8080/api/auth/login
Content-Type: application/json

  {
    "username": "admin",
    "password": "admin123"
  }

### 2) Tạo mới vehicle (ADMIN)
POST http://localhost:8080/api/vehicles
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "title": "Toyota Camry 2023",
  "vehicleType": "SEDAN",
  "licensePlate": "30A-123.45",
  "dailyPrice": 500000,
  "currency": "VND",
  "description": "Xe sạch, mới, tiết kiệm xăng"
}

### 3) Danh sách vehicles (PUBLIC)
GET http://localhost:8080/api/vehicles

### 4) Lấy chi tiết vehicle theo ID (PUBLIC)
GET http://localhost:8080/api/vehicles/{{vehicle_id}}

### 5) Cập nhật vehicle (ADMIN)
PUT http://localhost:8080/api/vehicles/{{vehicle_id}}
Authorization: Bearer {{admin_token}}
Content-Type: application/json

{
  "title": "Toyota Camry 2023 - Updated",
  "vehicleType": "SEDAN",
  "licensePlate": "30A-123.45",
  "dailyPrice": 520000,
  "currency": "VND",
  "description": "Đã vệ sinh nội thất"
}

### 6) Xóa vehicle (ADMIN)
DELETE http://localhost:8080/api/vehicles/{{vehicle_id}}
Authorization: Bearer {{admin_token}}

### 7) Tạo booking (USER)
POST http://localhost:8080/api/bookings
Authorization: Bearer {{user_token}}
Content-Type: application/json

{
  "vehicleId": {{vehicle_id}},
  "startAt": "{{start_iso}}",
  "endAt": "{{end_iso}}",
  "notes": "Thuê cuối tuần"
}

### 8) Xem booking của tôi (USER)
GET http://localhost:8080/api/bookings/me
Authorization: Bearer {{user_token}}

### 9) Admin confirm booking
POST http://localhost:8080/api/bookings/{{booking_id}}/confirm
Authorization: Bearer {{admin_token}}

### 10) Admin activate booking
POST http://localhost:8080/api/bookings/{{booking_id}}/activate
Authorization: Bearer {{admin_token}}

### 11) Admin complete booking
POST http://localhost:8080/api/bookings/{{booking_id}}/complete
Authorization: Bearer {{admin_token}}

### 12) Cancel booking (USER or ADMIN)
POST http://localhost:8080/api/bookings/{{booking_id}}/cancel
Authorization: Bearer {{user_token}}


